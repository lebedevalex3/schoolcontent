# Вариант 23, задание 3

LaTeX-исходник самодостаточен. Канонический TikZ-рисунок хранится в `latex/figure.tex`.

```bash
mkdir -p outputs
pdflatex -interaction=nonstopmode -halt-on-error -output-directory=outputs latex/main.tex
```

Готовый файл: `outputs/main.pdf`.

